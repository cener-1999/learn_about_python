'''
1.通过SQLAlchrmy操作数据库，只需操作映射到数据库的对象即可，
  具体的数据库操作会通过数据库对象映射到数据库

2.把table映射成class，把行映射为实例，把字段映射为属性

3.用ORM(对象关系映射)把关系模型映射为对象模型

4.为了使python连接DB，需要一个驱动，这个驱动是用于与DB交换的库。

5.连接对象和游标对象
  连接对象：DB连接对象，建立连接  pymysql.connect(参数)
  游标对象：游标对象用于执行、查询和获取结果
  
  Connection()是与数据库的网络连接，它的唯一真正用途是返回游标
  
  连接对象是您与数据库的连接，完成与数据库的所有连接后，请关闭该对象。
  游标对象是查询结果集的迭代器。完成该结果集后，关闭它们。
 ''' 
 
  conn = pymysql.connect(
    host='127.0.0.1',
    port = 3306,
    user='root',
    passwd='',
    db='test',
    charset='utf8'
)

session.add_all([ojb1,ojb2...])#可以调用多个对象